The runcard's `sequence_timeout` field (on `QbloxADCSequencer`) is deprecated and will be removed in a future release; setting it now emits a `FutureWarning`. It has no effect on the instrument's behavior. `Parameter.SEQUENCE_TIMEOUT` has also been removed, so it can no longer be set at runtime `set_parameter` either.
