`Platform.session()` now yields a `Session` object (`with platform.session() as session: ...`) exposing `execution_time`, `success`, and `error` for the code run inside the `with` block, populated whether it completes successfully or raises. `execution_time` is also logged via `logger.info`.
