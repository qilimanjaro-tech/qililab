Cleaned up dependency declarations with `deptry`: promoted packages that were already used directly but only pulled in transitively (`cloudpickle`, `ipython`, `loguru`, `matplotlib`, `numpy`, `plotly`, `pydantic`, `scipy`) to direct dependencies, and dropped unused ones (`papermill`, `rustworkx`). `deptry` now runs in CI on every PR to keep this from drifting again.
