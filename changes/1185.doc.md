Documented the development workflow in the README: environment setup with `uv`, linting/formatting with `ruff` and `mdformat`, type checking with `mypy`, running tests with `pytest`, and the new towncrier-based changelog process.
