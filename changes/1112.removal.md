Removed the `external_trigger` parameter from the Qblox cluster controller instrument in the runcard. `QbloxClusterController.set_ext_trigger` is now raised internally whenever a QProgram contains a `wait_trigger` using the trigger channel 15 (the last one).
