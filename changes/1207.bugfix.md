Fixed deserialization of YAML files (e.g. `QProgram`s) that were serialized by qililab versions still depending on `qilisdk`: such files can contain a `!tuple` tag that qililab's own YAML loader did not know how to construct, causing a `ConstructorError`. A backward-compatible constructor for `!tuple` has been added so these older files load correctly again.
