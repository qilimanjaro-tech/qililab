Fixed the Keysight E5080B driver blocking for 5 seconds merely by being imported: the `time.sleep(5)` sat in the class body, so it ran at class-definition time (whenever this driver's module was loaded). It has been removed.
